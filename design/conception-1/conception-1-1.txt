@startuml
class Main

package Model {

  class Jeu{
    + void deplacement(int direction)
  }
  class Plateau{
    + boolean allerNord(int x, int y)
    + boolean allerSud(int x, int y)
    + boolean allerEst(int x, int y)
    + boolean allerOuest(int x, int y)
  }
  class Heros{
    + boolean deplacement(Plateau plateau, int direction)
  }

  abstract class Composant {
    # int posX
    # int posY
  }

  abstract class Statique{
    + abstract String getType()
  }

  abstract class Dynamique {
    + void deplacementGauche()
    + void deplacementHaut()
    + void deplacementBas()
    + void deplacementDroite()
  }

  class Mur{
    + String getType()

  }

  Jeu --* Plateau
  Jeu --* Heros
  Plateau --* Statique
  
  Composant <|-- Statique
  Composant <|-- Dynamique
  Dynamique <|-- Heros
  
}

package Vue {
  class View
}

package Controleur {
  class Controleur
}

interface Observer
class Observable

Observer <|.. View : "<<realizes>>"
Observable <|-- Jeu

Controleur "0..*" <- View
Observer "0..*" <-- Observable

Jeu "1" <-- Main
Jeu "1" <-- View
Jeu "1" <-- Controleur
Mur -|> Statique
@enduml